# Copy this file to credentials.py and fill in your values.
# credentials.py is git-ignored and will not be uploaded.

# ImSor API credentials (the admin account created during first-time setup)
username = "admin"
password = "changeme"

# Google OAuth credentials (from https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID = "changeme"
GOOGLE_CLIENT_SECRET = "changeme"
