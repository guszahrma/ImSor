# Copy this file to credentials.py and fill in your values.
# credentials.py is git-ignored and will never be committed.

# ImSor central-service account — used by imsor-web and auto-annotator
# to authenticate to the central-service API.
central_service_username = "changeme"
central_service_password = "changeme"

# Google OAuth application credentials — used by imsor-web only.
# Obtain from https://console.cloud.google.com/apis/credentials
google_client_id = "changeme"
google_client_secret = "changeme"
